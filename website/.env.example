# Payload secret — set a strong random value in production
PAYLOAD_SECRET=pkt-dev-secret-change-me
# SQLite file in dev; swap payload.config.ts to Postgres for production
DATABASE_URI=file:./pkt.db
# Public origin of the site
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
