import {category} from './category'
import {machine} from './machine'
import {catalogImage, richText, seo, specification} from './objects'

export const schemaTypes = [category, machine, catalogImage, specification, seo, richText]
